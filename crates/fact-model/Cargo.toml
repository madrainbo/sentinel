[package]
name = "fact-model"
version = "0.1.0"
edition = "2021"

[dependencies]
sha2 = "0.10"
# Used only by the YAML alias-bomb guard in `limits` (event-level token count,
# no tree materialization). The YAML parsers depend on the same version.
yaml-rust2 = "0.11.0"
