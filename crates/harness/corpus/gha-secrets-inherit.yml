# Calling a reusable workflow with `secrets: inherit` passes ALL caller secrets.
# EXPECT: Medium GHA-SECRETS-INHERIT
on: push
jobs:
  deploy:
    uses: my-org/shared/.github/workflows/deploy.yml@v1
    secrets: inherit
